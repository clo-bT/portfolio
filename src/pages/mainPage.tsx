import styled from "styled-components";

const MainPageContainer = styled.div``;
const MainPage = () => {
  return (
    <MainPageContainer>
      
    </MainPageContainer>
  )
};

export default MainPage;
